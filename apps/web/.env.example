NEXT_PUBLIC_API_URL=http://localhost:8000

# No auth/login flow yet (see api.py's module docstring) -- the frontend
# is told which user_id to act as via this env var. Get it from
# apps/agent/scripts/seed.py's output, or your own Supabase auth.users id.
NEXT_PUBLIC_DEMO_USER_ID=
