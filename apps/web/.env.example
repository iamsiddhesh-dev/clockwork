NEXT_PUBLIC_API_URL=http://localhost:8000

# Same Supabase project as apps/agent -- anon key is safe to expose
# client-side by design (RLS/auth gate access, not secrecy of this key).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
